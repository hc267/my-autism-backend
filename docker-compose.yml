services:
  db:
    image: postgres:15
    container_name: autism_postgres
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: autism_db
    ports:
      - "5432:5432"